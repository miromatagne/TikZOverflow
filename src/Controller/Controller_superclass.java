package Controller;

/*
    This is a SuperClass for all the controller, it possess an abstract method update which will be called by
    the screen Handler whenever a scene (fxml) associated with this controller needs to be update/reload.
 */

abstract public class Controller_superclass {
    abstract public void update();
}
